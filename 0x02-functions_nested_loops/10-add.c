#include "main.h"
#include <stdio.h>
/**
 * main -  a function that adds two integers and returns the result
 *
 * Return: result
 */
int add(int, int)
{
	int a;
	int b;

	result = a + b;
	return (result);
}
